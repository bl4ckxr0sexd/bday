{
  "name": "birthday-invite",
  "version": "1.0.0",
  "private": true,
  "description": "Configurable birthday invitation email + landing page with visitor tracking.",
  "scripts": {
    "build": "node build.js"
  }
}
